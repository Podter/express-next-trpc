{
  "name": "express-next",
  "description": "Next.js application with Express + tRPC as backend",
  "version": "1.0.0",
  "author": "Podter",
  "license": "MIT",
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean",
    "dev": "turbo run dev",
    "format": "prettier --write .",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "prettier": "^3.0.3",
    "shx": "^0.3.4",
    "turbo": "^1.10.13"
  }
}
