{
  "name": "express-next",
  "version": "1.0.0",
  "private": true,
  "author": "Podter",
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean",
    "dev": "turbo run dev",
    "format": "prettier --write .",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "prettier": "^3.0.3",
    "shx": "^0.3.4",
    "turbo": "^1.10.13"
  }
}
